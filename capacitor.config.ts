import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'com.example.laravelcapacitorboilerplate',
    appName: 'Laravel Capacitor Boilerplate',
    webDir: 'dist-capacitor',
};

export default config;
